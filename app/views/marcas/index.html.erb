<p id="notice"><%= notice %></p>
<p id="notice"><%= notice %></p>
<div class="container">
    <table class="table table-dark">
        <thead>
            <tr>
                <th>Id</th><th>Nombre</th><th colspan="3"></th>
                <th class="text-center"><%= link_to 'Nuevo', new_marca_path, :class => "btn btn-primary" %></th>
            </tr>
        </thead>
        <tbody>
          <% @marcas.each do |marca| %>
            <tr>
              <td><%= marca.nombre %></td>
              <td><%= link_to 'Mostrar', marca, :class => "btn btn-warning" %></td>
              <td><%= link_to 'Editar', edit_marca_path(marca), :class => "btn btn-success" %></td>
            </tr>   
          <% end %>
        </tbody>
    </table>
</div>
